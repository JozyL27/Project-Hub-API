type Query {
  greeting: String
  lists(id: ID!): [List]
  list(id: ID!): List
}

type Mutation {
  createList(input: CreateListInput): List
}

type List {
  id: ID!
  title: String
  author: String
  date_created: Int
}

input CreateListInput {
  title: String
  author: String
}
